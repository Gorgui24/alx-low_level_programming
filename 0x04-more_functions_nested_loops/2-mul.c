#include "main.h"
/**
 * mul - print multi equal to a * b
 * @a: first value
 * @b: second value 
 * Return: return multiplication of two numbers
 */
int mul(int a, int b)
{
	int multi = a * b;
	
	ireturn (multi);
}
