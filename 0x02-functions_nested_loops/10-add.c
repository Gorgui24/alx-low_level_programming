#include "main.h"
/**
 * add - print sum of two  numbers
 * @a: first number to add
 * @b: second number to add
 * Return: resturn sum of thes two numbers
 */
int add(int a, int b)
{
	int sum = a + b;
	
	return (sum);
}
