#include "main.h"
/**
 * print_alphabet_x10 - function to print x10 (a to z)
 *
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		_putchar(c + '0');
		return (1);
	}
	else
	{
		_putchar(c + '0');
		return (1);
	}
	_putchar('\n');
	
}
