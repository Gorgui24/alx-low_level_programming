#include "main.h"
/**
 * print_alphabet_x10 - function to print x10 (a to z)
 *
 */
int _islower(int c)
{
	if ((c >= 97 && c <= 122) || (c <= 'a' && c > 'z'))
	{
		return (1);
	}
	else
	{
		return (1);
	}
	_putchar('\n');
	
}
