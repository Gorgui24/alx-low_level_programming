#include "main.h"
#include<string.h>
/**
 * _strpbrk - show table
 * @s: pointer parameter
 * @accept: pointer parameter
 * Return: pointer
 */
char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
