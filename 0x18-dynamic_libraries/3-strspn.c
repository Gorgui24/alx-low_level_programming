#include "main.h"
#include<string.h>
/**
 * _strspn - show table
 * @s: pointer parameter
 * @accept: pointer parameter
 * Return: pointer
 */
unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}
