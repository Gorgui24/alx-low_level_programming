#include "main.h"
#include<string.h>
/**
 * _memset - show table 
 * @s: pointer parameter
 * @b: char parameter
 * @n: parameter int
 * Return: pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}
